<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ExamPlanner.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:ExamPlanner.Pages"
    Title="ExamPlanner">

    <ShellContent
        Title="Exams"
        ContentTemplate="{DataTemplate pages:ExamListPage}"
        Route="ExamListPage" />

</Shell>
